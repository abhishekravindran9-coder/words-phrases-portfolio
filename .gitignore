# Java / Maven
target/
*.class
*.jar
*.war

# IDE
.idea/
*.iml
.vscode/

# Secrets — never commit these
# application-prod.properties is safe to commit (uses only ${ENV_VAR} references)
backend/src/main/resources/application-local.properties
